'use strict';

//функция отслеживания нажатий на пины
mapPins.addEventListener('click', function(evt){
  const target = evt.target;
  const pressedPin = target.closest('.map__pin');
  console.log(dataResult.indexOf()); 
})

console.log(dataResult);